name: Deploy to Loopia

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: loopia-deploy
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      VITE_BASE_PATH: ${{ secrets.VITE_BASE_PATH || '/' }}
      FTP_HOST: ${{ secrets.FTP_HOST }}
      FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
      FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
      FTP_PATH_TEST: ${{ secrets.FTP_PATH_TEST }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build site dev.skillshot.se
        run: npm run build

      - name: Verify FTP secrets
        run: |
          [ -n "$FTP_HOST" ] || { echo "Missing FTP_HOST secret"; exit 1; }
          [ -n "$FTP_USERNAME" ] || { echo "Missing FTP_USERNAME secret"; exit 1; }
          [ -n "$FTP_PASSWORD" ] || { echo "Missing FTP_PASSWORD secret"; exit 1; }
          [ -n "$FTP_PATH_TEST" ] || { echo "Missing FTP_PATH_TEST secret"; exit 1; }

      - name: Upload to Loopia via FTP (dev.skillshot.se)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ env.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: ${{ env.FTP_PATH_TEST }}
          protocol: ftp
          local-dir: dist/
